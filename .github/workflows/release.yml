name: release

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - CHANGELOG.md

permissions:
  contents: write
  actions: write

jobs:
  sanity:
    runs-on: ubuntu-latest
    steps:
      - name: Show environment
        run: |
          echo "Workflow is valid and running."
          echo "GITHUB_REF: "
          echo "GITHUB_SHA: "